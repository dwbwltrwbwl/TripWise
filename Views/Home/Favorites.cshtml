@{
    ViewData["Title"] = "Избранное";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4" style="font-family: 'Nunito', sans-serif; font-weight: 800; font-size: 2.5rem; color: #333; color: #0379D9;">
                Избранное
            </h1>
            <p class="lead">Сохраненные направления, отели и маршруты</p>

            @if (Context.Session.GetInt32("UserId") == null)
            {
                <div class="alert alert-info">
                    <h5>Войдите в аккаунт</h5>
                    <p>Чтобы сохранять понравившиеся направления, войдите в свой аккаунт.</p>
                    <a asp-controller="Account" asp-action="Login" class="btn btn-primary">Войти</a>
                    <a asp-controller="Account" asp-action="Register" class="btn btn-outline-primary">Зарегистрироваться</a>
                </div>
            }
            else
            {
                <div class="card shadow-sm border-0">
                    <div class="card-body text-center py-5">
                        <i class="fas fa-heart text-muted mb-3" style="font-size: 3rem;"></i>
                        <h5>Пока ничего нет в избранном</h5>
                        <p class="text-muted">Сохраняйте понравившиеся направления, нажимая на сердечко</p>
                        <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Начать поиск</a>
                    </div>
                </div>
            }
        </div>
    </div>
</div>